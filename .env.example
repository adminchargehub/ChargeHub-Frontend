# Copy to .env.local for overrides. Never commit .env.local.

# Leave unset in development — vite.config.ts proxies /api to localhost:8000,
# which keeps the browser same-origin and takes CORS out of the picture.
# Set this to the deployed API origin for preview/production builds.
# VITE_API_URL=https://api.chargehub.example
